apiVersion: vlanman.dialo.ai/v1
kind: VlanNetwork
metadata:
  name: net4
spec:
  excludedNodes: ["nixos2"]
  gatewayIp: "192.168.100.4"
  localSubnet: "192.168.400.0/24"
  remoteSubnet: "192.168.500.0/24"
  vlanId: 400
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: vlan-manager-net4
  namespace: vlanman-system
  labels:
    vlanman.dialo.ai/manager: net4
spec:
  template:
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - nixos2
      containers:
        - env:
            - name: VLAN_ID
              value: "400"
          image: 192.168.10.201:5000/vlan-manager:latest-dev
          name: vlan-manager
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
                - NET_RAW
      hostPID: true
status:
  currentNumberScheduled: 1
  desiredNumberScheduled: 1
  numberAvailable: 1
  numberMisscheduled: 0
  numberReady: 1
---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    job-name: create-vlan-job-net4-nixos
  name: create-vlan-job-net4-nixos
  namespace: vlanman-system
spec:
  template:
    metadata:
      labels:
        batch.kubernetes.io/job-name: create-vlan-job-net4-nixos
        job-name: create-vlan-job-net4-nixos
    spec:
      containers:
        - env:
            - name: PID
            - name: ID
              value: "400"
          image: 192.168.10.201:5000/vlan-interface:latest-dev
          imagePullPolicy: Always
          name: create-vlan
          securityContext:
            capabilities:
              add:
                - NET_ADMIN
                - NET_RAW
      hostNetwork: true
      hostPID: true
      nodeSelector:
        kubernetes.io/hostname: nixos
status:
  ready: 0
  succeeded: 1
  terminating: 0